# Apps

- `web`: new production Next.js frontend
- `api`: new production backend scaffold
- `worker`: background jobs scaffold

The existing prototype remains in the repository root until the rebuild is complete.
