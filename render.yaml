services:
  - type: web
    name: career-pilot
    runtime: node
    repo: https://github.com/AswinGMX/Career-pilot
    branch: main
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm start
    autoDeploy: true
    envVars:
      - key: NODE_VERSION
        value: 20
